name: Extend Interactive Audit Coverage

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/extend-interactive-audit.yml'

permissions:
  contents: write

jobs:
  extend:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extend tests
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path=Path('scripts/interactive-audit.mjs')
          text=path.read_text(encoding='utf-8')

          old_game='''    const introButton = page.locator('#startShowBtn');
    if (await introButton.count() && await introButton.isVisible()) await introButton.click();
    await page.click('#openTournamentBtn');'''
          new_game='''    const introButton = page.locator('#startShowBtn');
    if (await introButton.count() && await introButton.isVisible()) await introButton.click();
    const homeView = page.locator('#home');
    if (await homeView.evaluate(element => element.classList.contains('hidden'))) {
      const homeButton = page.locator('[data-nav="home"]').first();
      if (await homeButton.isVisible()) await homeButton.click();
      else await page.evaluate(() => showView('home'));
      await page.waitForTimeout(120);
    }
    await page.click('#openTournamentBtn');'''
          if old_game not in text:
              raise SystemExit('Game launch block was not found.')
          text=text.replace(old_game,new_game,1)

          old_next='''    requireTrue((await page.textContent('#completeStep')).includes('Completed'), 'Step 10 completion did not survive reload.');
  });'''
          new_next='''    requireTrue((await page.textContent('#completeStep')).includes('Completed'), 'Step 10 completion did not survive reload.');
    await page.goto(url('new-believer-complete.html'));
    await waitForApp(page);
    await page.fill('#planRhythm', 'Read Scripture after breakfast.');
    await page.fill('#planChurch', 'Gather with the church each week.');
    await page.click('#savePlan');
    await page.reload();
    await waitForApp(page);
    requireTrue((await page.inputValue('#planRhythm')) === 'Read Scripture after breakfast.', 'Next-season plan did not survive reload.');
  });'''
          if old_next not in text:
              raise SystemExit('New Believer completion block was not found.')
          text=text.replace(old_next,new_next,1)

          old_close='''  });
  await page.close();
}

function section(title, items, empty) {'''
          new_close='''  });

  await runTest('Church game supports one-team play', async () => {
    await page.goto(url('games.html'));
    await waitForApp(page);
    await page.evaluate(() => localStorage.clear());
    await page.reload();
    await waitForApp(page);
    const introButton = page.locator('#startShowBtn');
    if (await introButton.count() && await introButton.isVisible()) await introButton.click();
    await page.click('#openTournamentBtn');
    await page.selectOption('#teamCount', '1');
    await page.dispatchEvent('#teamCount', 'change');
    await page.waitForTimeout(150);
    requireTrue(await page.locator('#teamNames input').count() === 1, 'Tournament setup did not create one team-name field.');
    await page.click('#beginTournamentBtn');
    await page.waitForTimeout(200);
    requireTrue(await page.locator('#dynamicScoreboard .team').count() === 1, 'Live scoreboard did not support one-team play.');
  });
  await page.close();
}

function section(title, items, empty) {'''
          if old_close not in text:
              raise SystemExit('Game test closing block was not found.')
          text=text.replace(old_close,new_close,1)

          path.write_text(text,encoding='utf-8')
          PY

      - name: Remove one-time workflow and commit
        run: |
          git rm -f .github/workflows/extend-interactive-audit.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Extend interactive feature audit coverage"
          git pull --rebase origin main
          git push
