name: Following Jesus for Yourself Audit

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'following-jesus-for-yourself.html'
      - 'following-jesus-for-yourself-known-by-god.html'
      - 'following-jesus-for-yourself.css'
      - 'styles.css'
      - 'script.js'
      - 'site-navigation.js'
      - 'scripts/following-jesus-for-yourself-audit.mjs'
      - '.github/workflows/following-jesus-for-yourself-audit.yml'
  push:
    branches: [main]
    paths:
      - 'following-jesus-for-yourself.html'
      - 'following-jesus-for-yourself-known-by-god.html'
      - 'following-jesus-for-yourself.css'
      - 'styles.css'
      - 'script.js'
      - 'site-navigation.js'
      - 'scripts/following-jesus-for-yourself-audit.mjs'
      - '.github/workflows/following-jesus-for-yourself-audit.yml'

permissions:
  contents: read

jobs:
  youth-study:
    name: Youth-safe content, responsive layout, and accessibility
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install audit tools
        run: |
          npm install --no-save playwright axe-core
          npx playwright install --with-deps chromium

      - name: Start local site
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/nldg-http.log 2>&1 &
          echo $! > /tmp/nldg-http.pid
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4173/following-jesus-for-yourself.html > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/nldg-http.log
          exit 1

      - name: Run Following Jesus for Yourself audit
        run: node scripts/following-jesus-for-yourself-audit.mjs

      - name: Upload audit report and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: following-jesus-for-yourself-audit
          path: following-jesus-for-yourself-audit-results
          if-no-files-found: error
