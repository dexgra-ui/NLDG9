name: Scripture Chess Prototype Audit

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/scripture-chess-prototype-audit.yml'
      - 'scripture-chess-prototype.html'
      - 'scripture-chess.css'
      - 'scripture-chess.js'
      - 'scripture-chess-questions.js'
      - 'SCRIPTURE-CHESS-THIRD-PARTY-NOTICE.md'
      - 'scripts/scripture-chess-audit.mjs'
      - 'scripts/scripture-chess-browser-audit.mjs'
      - 'games.html'
      - 'play.html'
      - 'sitemap.xml'
      - 'content-library.js'
  push:
    branches: [prototype-scripture-chess]
    paths:
      - '.github/workflows/scripture-chess-prototype-audit.yml'
      - 'scripture-chess-prototype.html'
      - 'scripture-chess.css'
      - 'scripture-chess.js'
      - 'scripture-chess-questions.js'
      - 'SCRIPTURE-CHESS-THIRD-PARTY-NOTICE.md'
      - 'scripts/scripture-chess-audit.mjs'
      - 'scripts/scripture-chess-browser-audit.mjs'
      - 'games.html'
      - 'play.html'
      - 'sitemap.xml'
      - 'content-library.js'

permissions:
  contents: read

jobs:
  prototype:
    name: Chess rules, Scripture events, scoring, keyboard, and mobile
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install deterministic test dependencies
        run: |
          npm install --no-save playwright chess.js@1.4.0
          npx playwright install --with-deps chromium

      - name: Run static prototype audit
        run: node scripts/scripture-chess-audit.mjs

      - name: Start local site
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/scripture-chess-http.log 2>&1 &
          echo $! > /tmp/scripture-chess-http.pid
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4173/scripture-chess-prototype.html > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/scripture-chess-http.log
          exit 1

      - name: Run browser behavior audit
        run: node scripts/scripture-chess-browser-audit.mjs

      - name: Upload prototype test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scripture-chess-prototype-audit
          path: scripture-chess-audit-results
          if-no-files-found: error
